<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head',{ title:'Confirm Remove' }) %>
  <body>
    <%- include('../partials/header') %>
    <h2>ADVERTENCIA</h2>
    <hr />
    <div class="row">
      <div class="col-12 col-md-10 mx-auto">
        <div class="card mb-3">
          <div class="card-body">
            <h3 class="text-center">
              Estas seguro que queres eliminar el curso: <%= title %>?
            </h3>
            <p class="text-center">No se podran revertir los cambios</p>
            <div class="d-flex justify-content-center gap-2 align-content-center">
                <a class="btn btn-secondary" href="/courses/detail/<%= id %>">Don't delete</a>
                <form action="/courses/remove/<%= id %>?_method=delete" method="post">
                <button type="submit" class="btn btn-danger">Confirm delete</button>
            </form>
            </div>

          </div>
        </div>
      </div>
    </div>
    <%- include('../partials/footer') %> <%- include('../partials/whatsapp') %>
  </body>
</html>
